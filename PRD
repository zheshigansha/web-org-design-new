# 内容监控与 AI 选题助手：更年期赛道专用版

> **更新记录：v2.0（终版）**
> - 数据采集：第三方平台 + 每日一次定时/手动触发
> - 分类管理：支持用户增删分类
> - 热度评判：平铺选项（点赞/阅读/互动/综合评分），用户可自定义
> - 报告生成：定时（每日一次）+ 手动两种模式
> - 选题池：去重机制、状态管理（待创作/已创作/已发布），支持自动+手动标记
> - 数据清理：统一90天，AI报告永久保留（可手动删除）
> - 监控设置：平铺选项（关键词/指定账号）
> - 冷启动：数据库为空时提示，不生成空报告

---

## 一、 项目定位
这是一个专为**更年期女性赛道**运营者设计的决策辅助工具。通过监控小红书和公众号的特定分类内容，利用 AI（OpenAI）进行深度分析，旨在解决"精准捕捉用户痛点"与"高效产出高质量选题"的难题。

## 二、 核心架构
### 1. 监控分类管理（顶层逻辑）
系统以"监控分类"为最小运作单元，每个分类配置独立的关键词、博主和 AI 报告。初始分类包括：
* **更年期心理/情感：** 关注情绪波动、家庭关系、自我认同等。
* **红参（产品/转化）：** 监控产品反馈、服用体验、市场评价。
* **更年期知识/科普：** 关注专家号、医生发布的医疗/营养建议。
* **生活方式/锻炼：** 关注瑜伽、冥想、睡眠管理等引流向内容。

### 2. 分类内部 Tab 结构
* **Tab 1：内容仓库**
    * **数据源：** 仅限"小红书"与"公众号"。
    * **展示：** 瀑布流或列表展示采集到的原文标题、摘要、封面图。
    * **筛选交互：**
        * **平台平铺：** 顶部两个大按钮切换"小红书"和"公众号"。
        * **7天时间轴：** 顶部横向滚动日期卡片，默认选中当天，支持点击查看过去 7 天内"新发现"的内容。
* **Tab 2：AI 选题分析与选题池**
    * **AI 洞察：** 提取当天该分类下热度最高的 Top 10 内容，通过 OpenAI 分析产生报告。
    * **功能点：** * **手动触发：** 提供"立即生成今日报告"按钮。
        * **选题建议字段：** 包含【方向标题】、【为什么做（痛点分析）】、【爆点与增长空间】。
    * **选题池功能：** 用户可对 AI 生成的选题点击"加入选题池"。已入池的选题将永久保留，作为后续创作的灵感库。
* **Tab 3：监控设置**
    * 配置当前分类的"监控目标"（关键词、指定博主/账号链接）。
    * 热度评判标准：平铺四个选项【点赞数】【阅读量】【互动率】【综合评分】，默认"综合评分"。
    * 报告生成频率：【定时每日一次】【手动触发】两种模式可选。

## 三、 数据逻辑（SQLite 驱动）
1.  **时效性处理：** 原始采集内容（Tab 1）统一保留 90 天，过期自动清理以保持系统轻量。
2.  **永久资产：** 所有的 AI 选题报告和"入池选题"永久保留，可手动删除。
3.  **分类隔离：** 数据库表结构需支持按 `category_id` 进行物理隔离。
4.  **冷启动保护：** 当数据库为空或内容不足时，AI 报告不生成，提示"数据库为空"。

## 四、 选题池管理
1.  **去重机制：** 同一选题被多次加入时，自动识别并跳过，不重复收录。
2.  **状态管理：** 选题共有三种状态【待创作】【已创作】【已发布】，支持手动标记和自动流转。
3.  **状态流转：** 用户手动创作 → 自动标记为"已创作"；用户发布后 → 手动标记为"已发布"。

## 五、 数据采集规格
1.  **采集来源：** 第三方平台 API（小红书、公众号）。
2.  **采集频率：** 每日定时一次 + 用户随时手动触发。
3.  **热度计算：** 支持四种模式平铺切换：【点赞数】【阅读量】【互动率】【综合评分】，用户可在监控设置中默认选择。

## 六、 设计与交互要求
1.  **风格：** 极简、专业，去除冗余的 UI 装饰，适合高频办公。
2.  **技术栈：** **Next.js** 轻量化实现，前端 UI 使用 Tailwind CSS。
3.  **核心体验：** * 拒绝下拉框，高频操作（平台、日期）全部平铺化。
    * 提供清晰的"数据加载中"状态，尤其是在 AI 生成报告期间。
